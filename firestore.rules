rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permitir lectura y escritura en la colección ClaroMediaAgenteIA
    match /ClaroMediaAgenteIA/{document=**} {
      allow read, write: if true;
    }
  }
}
